<table class="domains" summary="Profile information">
	<tr>
		<td class="main">
			<h1>All Domains</h1>
			Put some localizable content about domains here
<table>
  <tr>
  	<th></th>
    <th>Dewey Code</th>
    <th>Long name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
<%= will_paginate %>
<% @domains.each do |domain| %>
  <tr>
  	<td><%= image_tag domain.photo.url(:grav) %></td>
    <td><%= domain.code %></td>
    <td><%= domain.long_name %></td>
    <td><%= link_to 'Show', domain %></td>
    <td><%= link_to 'Edit', edit_domain_path(domain) %></td>
    <td><%= link_to 'Destroy', domain, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />
<%= will_paginate %>
<%= link_to 'New domain', new_domain_path %>
</td> <!-- end main class>

	</tr>
	
	
</table>